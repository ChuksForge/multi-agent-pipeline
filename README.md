# Multi-Agent Data Pipeline

Production-grade multi-agent pipeline using LangGraph, Claude, DuckDB, polars, and scikit-learn.

## Quick Start
```bash
cp .env.example .env
uv sync --extra dev
pytest
```
